<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.RPGInventoryMaster" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Tema para diálogos (DEFINIDO COMO ESTILO IRMÃO, NÃO ANINHADO) -->
    <style name="AppDialogTheme" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="android:windowBackground">@drawable/dialog_background</item>
        <item name="colorPrimary">#6200EE</item>
    </style>

    <!-- Tema principal que herda da base -->
    <style name="Theme.RPGInventoryMaster" parent="Base.Theme.RPGInventoryMaster" />
</resources>